steps:
  # Build Docker image
  - name: "gcr.io/cloud-builders/docker"
    dir: "speech-to-text/services/backend"
    entrypoint: bash
    args:
      - "-c"
      - |
        branch_name=$BRANCH_NAME
        branch=${branch_name//\//-}
        docker build -t ${_ARTIFACT_REGISTRY_CONTAINERS_URL}/speech-backend:latest \
          -t ${_ARTIFACT_REGISTRY_CONTAINERS_URL}/speech-backend:$SHORT_SHA \
          -t ${_ARTIFACT_REGISTRY_CONTAINERS_URL}/speech-backend:$branch \
          --network=cloudbuild .

  # Push all tags to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    dir: "speech-to-text/services/backend"
    entrypoint: docker
    args: [
      "push",
      "-a",
      "${_ARTIFACT_REGISTRY_CONTAINERS_URL}/speech-backend",
    ]

  # Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    dir: "speech-to-text/services/backend"
    entrypoint: gcloud
    args:
      - run
      - deploy
      - "speech-backend"
      - --image=${_ARTIFACT_REGISTRY_CONTAINERS_URL}/speech-backend:${SHORT_SHA}
      - --region=${_REGION}
      - --project=$PROJECT_ID
      - --service-account=sa-backend-dev@$PROJECT_ID.iam.gserviceaccount.com
      - --set-env-vars=GCP_PROJECT_ID=$PROJECT_ID,GCP_LOCATION=${_REGION},GCS_BUCKET_NAME=gcs-pj-speech-text-dev-audio-uploads,GCS_TRANSCRIPT_BUCKET=gcs-pj-speech-text-dev-transcripts,MAX_FILE_SIZE_MB=5120,RECOGNIZER_LOCATION=europe-west4,DEFAULT_RECOGNIZER_ID=dutch-recognizer-3,DEFAULT_LANGUAGE_CODE=nl-NL,SPEECH_MODEL=chirp_2,SIGNING_SERVICE_ACCOUNT=sa-backend-dev@$PROJECT_ID.iam.gserviceaccount.com
      - "--set-env-vars=CORS_ORIGINS=https://speech-frontend-396265930049.europe-west1.run.app;http://localhost:3000;http://localhost:3001"
      - --allow-unauthenticated
      - --cpu=2
      - --memory=4Gi
      - --max-instances=10
      - --min-instances=1
      - --port=8000
      - --timeout=3600

substitutions:
  _ARTIFACT_REGISTRY_CONTAINERS_URL: europe-west1-docker.pkg.dev/${PROJECT_ID}/pipeline-containers
  _REGION: europe-west1
  _PROJECT_NUMBER: ${PROJECT_NUMBER}

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
